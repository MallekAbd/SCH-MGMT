<% portalType='parent'; portalLinks=[{href:'/parent/dashboard',match:'/dashboard',icon:'home',label:'Accueil'},{href:'/parent/grades',match:'/grades',icon:'star-half-alt',label:'Notes'},{href:'/parent/attendance',match:'/attendance',icon:'clipboard-check',label:'Présences'},{href:'/parent/invoices',match:'/invoices',icon:'file-invoice',label:'Factures'},{href:'/parent/messages',match:'/messages',icon:'envelope',label:'Messages'}]; %>
<%- include('../../partials/portal-shell-start') %>
<h4 class="fw-bold mb-3">Notes de mes enfants</h4>
<% (childGrades || []).forEach(c => { %>
<div class="card border-0 shadow-sm mb-3"><div class="card-body">
<h6 class="fw-bold"><%= c.student?.firstName %> <%= c.student?.lastName %></h6>
<table class="table table-sm mb-0"><thead><tr><th>Matière</th><th>Examen</th><th class="text-end">Note</th></tr></thead><tbody>
<% (c.grades || []).forEach(g => { %><tr><td><%= g.course?.name %></td><td><%= g.exam?.name || '—' %></td><td class="text-end fw-bold <%= g.score>=10?'text-success':'text-danger' %>"><%= g.score %>/<%= g.maxScore %></td></tr><% }) %>
<% if (!c.grades || !c.grades.length) { %><tr><td colspan="3" class="text-center text-muted">Pas encore de notes</td></tr><% } %>
</tbody></table>
</div></div>
<% }) %>
<% if (!childGrades || !childGrades.length) { %><p class="text-muted">Aucune donnée</p><% } %>
<%- include('../../partials/portal-shell-end') %>
